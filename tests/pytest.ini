[pytest]
pythonpath = ..
markers =
    unit: tests a single function or class in isolation
    integration: black-box shape-contract tests across components (GPT.forward, prefill, decode_one_token)
    system: end-to-end tests (GPT.generate)
    nonfunctional: performance / latency benchmarks
